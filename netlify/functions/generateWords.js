exports.handler=async function(event){if(event.httpMethod!=="POST")return jsonResponse(405,{error:"Method not allowed"});const apiKey=process.env.OPENAI_API_KEY;if(!apiKey)return jsonResponse(501,{error:"OPENAI_API_KEY is not configured.",entries:[]});let body;try{body=JSON.parse(event.body||"{}")}catch{return jsonResponse(400,{error:"Invalid JSON"})}const theme=String(body.theme||"general knowledge").slice(0,120);const difficulty=Math.min(5,Math.max(1,Number(body.difficulty||2)));const style=String(body.style||"learning").slice(0,40);const maxWords=Math.min(12,Math.max(8,Number(body.maxWords||12)));const system=`You generate compact educational crossword content. Return only valid JSON. No markdown. Answers must be single words, A-Z only, 3 to 9 letters, no spaces, no punctuation. Prefer words that share letters so a 9x9 criss-cross grid can place them. Each clue should fit the requested style and difficulty. Difficulty 1 = common/direct. 5 = obscure/indirect/expert. Include a short educational note for each answer.`;const user={task:"Generate entries for a 9x9 criss-cross crossword.",theme,difficulty,style,count:maxWords,required_schema:{entries:[{answer:"WORD",clue:"brief clue",note:"brief educational note"}]}};try{const response=await fetch("https://api.openai.com/v1/chat/completions",{method:"POST",headers:{Authorization:`Bearer ${apiKey}`,"Content-Type":"application/json"},body:JSON.stringify({model:process.env.OPENAI_MODEL||"gpt-4o-mini",temperature:.8,messages:[{role:"system",content:system},{role:"user",content:JSON.stringify(user)}],response_format:{type:"json_object"}})});if(!response.ok){const text=await response.text();return jsonResponse(502,{error:"OpenAI API error",detail:text.slice(0,500)})}const data=await response.json();const content=data.choices?.[0]?.message?.content||"{}";const parsed=JSON.parse(content);return jsonResponse(200,{entries:cleanEntries(parsed.entries||[])})}catch(error){return jsonResponse(500,{error:error.message})}};function cleanEntries(entries){const seen=new Set();return entries.map(e=>({answer:String(e.answer||"").toUpperCase().replace(/[^A-Z]/g,""),clue:String(e.clue||"").trim(),note:String(e.note||"").trim()})).filter(e=>e.answer.length>=3&&e.answer.length<=9&&e.clue).filter(e=>{if(seen.has(e.answer))return false;seen.add(e.answer);return true}).slice(0,12)}function jsonResponse(statusCode,body){return{statusCode,headers:{"Content-Type":"application/json","Access-Control-Allow-Origin":"*"},body:JSON.stringify(body)}}
